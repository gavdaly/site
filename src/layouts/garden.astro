---
import Base from "./Base.astro";

const { frontmatter } = Astro.props;
---

<Base title={`${frontmatter.title} | GavDev Garden`}>
    <Fragment slot="head">
        <meta name="description" content={frontmatter.description} />
    </Fragment>
    <slot />
</Base>
